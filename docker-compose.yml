version: '3.8'

services:
  atts:
    build: .
    container_name: atts
    restart: always
    volumes:
      - .:/app
    ports:
      - "8000:8000"

    command: gunicorn root.wsgi:application --bind 0.0.0.0:8000 --workers 1 --log-level info